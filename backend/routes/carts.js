const express = require("express");
const router = express.Router();

router.get("/api/users/:id/carts", (req, res, next) => {});
router.post("/api/users/:id/carts", (req, res, next) => {});

module.exports = router;
