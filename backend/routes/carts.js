const express = require("express");
const router = express.Router();

router.get("/api/users/:id/cart", (req, res, next) => {});
router.post("/api/users/:id/cart", (req, res, next) => {});

module.exports = router;
